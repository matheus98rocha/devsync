import React from 'react'
import ReactLoading, { LoadingProps } from 'react-loading'

const Loading = () => (
  <ReactLoading type={'spin'} color={'#008021'} height={'2%'} width={'2%'} />
)

export default Loading
