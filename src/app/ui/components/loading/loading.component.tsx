import React from 'react'
import ReactLoading, { LoadingProps } from 'react-loading'

const Loading = () => (
  <ReactLoading type={'spin'} color={'#008021'} height={'5%'} width={'5%'} />
)

export default Loading
