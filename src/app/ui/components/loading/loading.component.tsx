import React from 'react'
import ReactLoading, { LoadingProps } from 'react-loading'

const Loading = () => (
  <ReactLoading type={'spin'} color={'#008021'} height={'20%'} width={'20%'} />
)

export default Loading
